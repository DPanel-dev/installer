# 选择语言
+ 简体中文
+ Enlish

# 选择操作
+ 安装面板
+ 升级面板
+ 卸载面板

# 安装面板
## 环境检测
> 1、 需要检测当前用户是否安装了 docker  或是  podman 
>
> 2、当前的执行用户是否有权限执行 docerk 或是 podman 命令，比如 docker ps
>
> 3、当前用户是否能请求到 dpanel/dpanel 仓库，如果请求不到默认下面使用 aliyun 的镜像
>

环境检测输出当前用户的环境信息，比如操作信息，docker / podman 版本等等基础信息

## 安装 Docker 
> 1、 在环境检测中如果检测到用户没有安装 docker / podman 才会提示此步骤
>
> 2、用户也可以选择不安装 docker 那么下方只能通过 二进制的方式安装，不能使用容器方式安装
>

提示用户是、否需要安装 docker 

## 安装方式
> 没有 docker / podman 的时候只能选择通过二进制安装
>

+ 容器安装
+ 二进制安装

## 选择版本
> 1、 说明文字引导用户访问 专业版 的链接，查看购买方式及差异
>
> 2、开发版更新比较激进，会随着开发流程及时发布功能
>

+ 社区版
+ 专业版
+ 开发版

## 选择版本
> 说明文字提示三个版本的差别，标准版提供 nginx 转发及 https 证书申请，需要绑定 80 及 443 端口，精简版则不包含以上功能，只有容器管理功能
>

+ 标准版  
提供 Nginx 转发及证书申请，需要绑定 80 及 443
+ 精简版  
只提供容器管理功能

## 选择构建系统
> 说明文字提示这两种基础镜像的差异。debian 基于 Libc 更稳定一些，alpine 基于 Musl 更轻量一些
>

+ alpine
+ debian

## 选择镜像
> 1、根据上方的环境检测自动选择镜像源，当然用户也可以强制选择
>

+ AliYUn
+ Hub

## 配置 Docker / Podman 连接地址
> 1、sock 地址需要先检测文件是否存在
>
> 2、远程地址需要检测连通性
>
> 3、提示信息还需要说明 docker.sock 和 podman.sock 文件的一些基础说明
>
> 4、ssh、tcp 连接地址应该是 ip:port 
>

+ 本地 sock 文件
+ 远程 tcp 连接
+ 远程 ssh 连接

## 是否使用 TLS 连接？
> 1、 仅限于用户选择通过 tcp 连接方式
>
> 2、生成证书完成后，存储到当前目前，并提示用户如果配置 docker 服务端
>

提示用户是否生成 tls 证书用于配置 tcp 连接权限

## 配置 ssh 连接权限
> 1、仅限于用户择通过 ssh 连接方式
>

支持输入用户名，密码，证书的方式

## 配置面板 dns 地址
> 1、仅限于用户选择通过容器的方式安装
>
> 2、提示容器内无法解析域名时, 请手动配置 DNS 地址, 为空时为不配置
>

默认不填写

## 配置面板容器名称
> 1、仅限于用户选择通过容器的方式安装
>
> 2、需要检测容器名称是否已经存在，提示用户更改名称
>

默认 dpanel

## 配置面板代理地址
> 提示用户代理地址用于面板内访问外部链接，例如拉取代码等操作。拉取镜像加速请配置 Docker daemon.json
>

默认为空不填写

## 配置面板数据存储目录
> 1、需要检测目录，不为空时提示用户无法安装
>

默认使用 /home/dpanel

## 配置面板访问端口
默认随机端口

# 升级面板
## 指定版本号
输入版本号

## 自动检测安装方式
> 1、检测用户通过哪种方式安装的
>
> 2、是通过容器？还是二进制
>
> 3、如果有多个容器获取出来，让用户选择要操作哪个
>
> 4、如果是二进制需要获取运行的目录在哪里
>
> 5、判断当前系统是 linux 还是 windows 二进制的更新方式不同
>

## 备份旧容器
> 1、仅限于容器安装时候
>
> 2、旧容器停止，以当前日期为后缀改名
>

提示用户是、否备份旧容器

## 升级容器
> 1、仅限于容器安装
>
> 2、获取出当前容器安装时候指定过的配置，还原命令重新拉取镜像创建
>

## 升级二进制
> 1、根据当前系统，通过 oci 的方式获取到最新的容器安装包，替换到当前运行的
>

# 卸载面板
与升级面板流程相似，先检测环境，列出来面板容器或是二进制程序。提示用户卸载哪个。

提示用户是否删除相关数据

# 安装
> 1、如果采用容器的方式安装生成 docker run 或是 podman run 命令
>
> 2、除了上方的配置外，基础命令如下
>
> 3、如果是二进制的方式安装，则下载对应的 镜像 oci 包，然后提取出 二进制文件 和 config.yaml 
>

## 创建容器
```plain
docker run -d name 容器名称 --restart on-failure:5 --log-driver json-file  --log-opt max-size=5m --log-opt max-file=10  --hostname 容器名称.pod.dpanel.local --add-host host.dpanel.local:host-gateway -e APP_NAME=容器名称 -e HTTP_PROXY=代理地址 -e HTTPS_PROXY=代理地址 --dns dns地址 -p 绑定端口:8080 -p 80:80 -p 443:443 -v sock文件::/var/run/docker.sock -v 数据存储目录:/dpanel 镜像
```

## 二进制方式
```plain
export STORAGE_LOCAL_PATH=数据存储目录
export APP_SERVER_PORT=运行端口
export HTTP_PROXY=
export HTTPS_PROXY=
dpanel server:start -f config.yaml
```





